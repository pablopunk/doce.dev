## A bunch of prompts I want to throw at the AI. Each paragraph is a different prompt.

Now that we can rename projects, it's possible to insert spaces in the name. Let's make it possible to also generate these with AI, i.e. right now it creates the slug-name (which is needed still) but it could name projects like this "My new project" and the slug could still be filled my-new-project. Get it? Let's do it.

I want to make this app multi-user. Explore login, actions, projects, settings. Think about types of users, and permissions. Let's make it as simple as possible at first, i.e. admin vs regular user. Admins could have access to everything and users won't have access to stuff like tailscale config, providers, etc. Let's plan first.

I want to think about action-level rate limiting. Let's plan.
