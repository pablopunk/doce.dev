services:
  app:
    image: node:20
    working_dir: /app
    volumes:
      - ./:/app
    command: ["sh", "-c", "pnpm install && pnpm run build && pnpm run preview -- --host 0.0.0.0 --port 3000"]
    environment:
      NODE_ENV: production
