services:
  app:
    image: node:20
    working_dir: /app
    volumes:
      - ./:/app
    command: ["sh", "-c", "pnpm install && pnpm run dev -- --host 0.0.0.0 --port 3000"]
    environment:
      NODE_ENV: development
