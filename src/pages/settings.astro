---
import { SettingsWorkspace } from "@/components/settings/SettingsWorkspace";
import AppLayout from "@/layouts/AppLayout.astro";
import { getSettingsLogsPageData } from "@/server/settings/logs";

const user = Astro.locals.user;
if (!user) {
	return Astro.redirect("/login");
}

const tabParam = Astro.url.searchParams.get("tab");
// Legacy tab IDs redirect to "general"
const legacyGeneralTabs = ["base-url", "account", "danger"];
// Status tab moved to Monitor page
if (tabParam === "status") {
	return Astro.redirect("/monitor?tab=queue");
}

const isProduction =
	import.meta.env.PROD || process.env.NODE_ENV === "production";
const initialTab =
	tabParam === "general" ||
	tabParam === "providers" ||
	tabParam === "mcps" ||
	tabParam === "skills" ||
	(tabParam === "logs" && isProduction)
		? tabParam
		: legacyGeneralTabs.includes(tabParam ?? "")
			? "general"
			: "providers";

const logsData = isProduction ? await getSettingsLogsPageData() : undefined;
---

<AppLayout title="Settings">
	<main class="container mx-auto max-w-6xl p-4 md:p-8">
		<div class="mb-6 md:mb-8 flex items-center justify-between">
			<div>
				<h1 class="text-2xl md:text-3xl font-bold">Settings</h1>
				<p class="text-muted-foreground text-sm md:text-base">Manage your doce.dev configuration</p>
			</div>
		</div>

		<SettingsWorkspace
			initialTab={initialTab}
			isProduction={isProduction}
			logsData={logsData}
			client:load
		/>
	</main>
</AppLayout>
