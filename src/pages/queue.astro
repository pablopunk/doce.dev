---
const redirectUrl = new URL("/settings", Astro.url);
redirectUrl.search = Astro.url.searchParams.toString();
redirectUrl.searchParams.set("tab", "status");

return Astro.redirect(redirectUrl.toString());
---
