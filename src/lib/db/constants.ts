/**
 * Database constants
 */

export const DB_TABLES = {
	CONFIG: "config",
	USERS: "users",
	PROJECTS: "projects",
	CONVERSATIONS: "conversations",
	MESSAGES: "messages",
	FILES: "files",
	DEPLOYMENTS: "deployments",
	MIGRATIONS: "migrations",
} as const;
